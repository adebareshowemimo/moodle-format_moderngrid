name: Moodle Plugin CI

on:
  push:
    branches: [main, develop]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:${{ matrix.postgres-version }}
        env:
          POSTGRES_USER: postgres
          POSTGRES_HOST_AUTH_METHOD: trust
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      mariadb:
        image: mariadb:10.11
        env:
          MYSQL_USER: root
          MYSQL_ALLOW_EMPTY_PASSWORD: "true"
          MYSQL_CHARACTER_SET_SERVER: utf8mb4
          MYSQL_COLLATION_SERVER: utf8mb4_unicode_ci
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    strategy:
      fail-fast: false
      matrix:
        include:
          - php: '8.1'
            moodle-branch: MOODLE_405_STABLE
            database: pgsql
            db-user: postgres
            postgres-version: '14'
          - php: '8.3'
            moodle-branch: MOODLE_502_STABLE
            database: pgsql
            db-user: postgres
            postgres-version: '16'
          - php: '8.3'
            moodle-branch: MOODLE_502_STABLE
            database: mariadb
            db-user: root
            postgres-version: '16'

    steps:
      - name: Check out plugin
        uses: actions/checkout@v7
        with:
          path: plugin

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: mbstring, pgsql, mysqli, zip, gd, soap, intl, sodium, exif
          ini-values: max_input_vars=5000
          coverage: none

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Initialise moodle-plugin-ci
        run: |
          composer create-project -n --no-dev --prefer-dist moodlehq/moodle-plugin-ci ci ^4
          echo "$(cd ci/bin; pwd)" >> "$GITHUB_PATH"
          echo "$(cd ci/vendor/bin; pwd)" >> "$GITHUB_PATH"
          sudo locale-gen en_AU.UTF-8
          echo "NVM_DIR=$HOME/.nvm" >> "$GITHUB_ENV"

      - name: Install Moodle
        run: moodle-plugin-ci install --plugin "$GITHUB_WORKSPACE/plugin" --db-host=127.0.0.1 --db-user="${{ matrix.db-user }}"
        env:
          DB: ${{ matrix.database }}
          MOODLE_BRANCH: ${{ matrix.moodle-branch }}

      - name: PHP Lint
        run: moodle-plugin-ci phplint "$GITHUB_WORKSPACE/plugin"

      - name: PHP Mess Detector
        continue-on-error: true
        run: moodle-plugin-ci phpmd "$GITHUB_WORKSPACE/plugin"

      - name: Moodle Code Checker
        run: moodle-plugin-ci codechecker --max-warnings 0 "$GITHUB_WORKSPACE/plugin"

      - name: Moodle PHPDoc Checker
        run: moodle-plugin-ci phpdoc "$GITHUB_WORKSPACE/plugin"

      - name: Validate plugin
        run: moodle-plugin-ci validate "$GITHUB_WORKSPACE/plugin"

      - name: Check upgrade savepoints
        run: moodle-plugin-ci savepoints "$GITHUB_WORKSPACE/plugin"

      - name: Mustache Lint
        continue-on-error: true
        run: moodle-plugin-ci mustache "$GITHUB_WORKSPACE/plugin"

      - name: Grunt
        continue-on-error: true
        run: moodle-plugin-ci grunt --max-lint-warnings 0 "$GITHUB_WORKSPACE/plugin"

      - name: PHPUnit tests
        run: moodle-plugin-ci phpunit --fail-on-warning "$GITHUB_WORKSPACE/plugin"
